package serialize_collection;

import java.util.Collection;
import java.util.Iterator;

/**
 * Created by andrew on 18.06.2017.
 */
public class MySerializer<T>
{
    /*
    public Collection<T> cloneCollection(Collection<T> col)
    {
        Iterator<T> it = col.iterator();
        while(it.hasNext())
        {
            T val = it.next();

            if (val instanceof Cloneable)
            {
                Object cloned = val.clone();
            }
        }
    }
    */
}
